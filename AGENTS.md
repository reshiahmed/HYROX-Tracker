# Project Instructions

- After all work for each user request is fully complete, and right before ending response, run `~/bin/codex-done` once. Do not play sound more than once or too early.
- Never deploy without explicit user permission for that specific deployment.
